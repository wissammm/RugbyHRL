[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rugbyhrl"
version = "0.1.0"
description = "Basic RugbyHRL project"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Wissam Boussella", email = "wissamboussella@gmail.com" }
]
requires-python = ">=3.8"
dependencies = [
    "gymnasium>=1.1.1",
    "matplotlib>=3.7.5",
    "numpy>=1.21",
    "pygame>=2.6.1",
    "pyvirtualdisplay>=3.0",
    "swig>=4.4.1",
    "tensordict>=0.6.2",
    "torch>=2.5.1",
]


[tool.setuptools.packages.find]
where = ["src"]
